import { t } from './common';

/**
 * <PropList> compact test-runner.
 */
export const PropListTestRunner = {
  /**
   * Generates a <PropList> item for running unit tests.
   */
  item(getTests: Promise<t.TestSuiteModel>) {
    /**
     * TODO 🐷
     */
  },
};
