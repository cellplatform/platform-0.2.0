import { Dev, expect } from './common';

export default Dev.describe('MonacoCrdt', (e) => {
  e.it('tmp', async (e) => {
    expect(123).to.eql(123);
  });
  e.it('tmp', async (e) => {
    expect(123).to.eql(123);
  });
  e.it('tmp', async (e) => {
    expect(123).to.eql(123);
  });
  e.it('tmp', async (e) => {
    expect(123).to.eql(123);
  });
  e.it('tmp', async (e) => {
    expect(123).to.eql(123);
  });
});
