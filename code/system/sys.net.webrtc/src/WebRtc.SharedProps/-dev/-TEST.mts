import { WebRtcSharedProps } from '..';
import { UserAgent, Dev, expect, slug, t } from '../../test.ui';

export default Dev.describe('Network: Shared Props', (e) => {
  //

  e.it('foo', async (e) => {
    /**
     * TODO 🐷
     */
  });
});
