import { describe, it, expect } from '../test/index.mjs';
import { WorkerPump } from './index.mjs';

describe('WorkerPump', () => {
  it('main', () => {
    //
    /**
     * TODO 🐷
     */
    console.log('On Main', WorkerPump);
  });

  it('worker', () => {
    /**
     * TODO 🐷
     */
    console.log('On Worker', WorkerPump);
  });
});
