/**
 * @external
 */
import { equals, is, isEmpty, isNil, pipe, reject, startsWith, uniq, toString, clone } from 'ramda';
export const R = {
  equals,
  is,
  isEmpty,
  isNil,
  pipe,
  reject,
  startsWith,
  uniq,
  toString,
  clone,
} as const;
