/**
 * @external
 */
import { clone, equals, is, isEmpty, isNil, pipe, reject, startsWith, toString, uniq } from 'ramda';
export const R = {
  clone,
  equals,
  is,
  isEmpty,
  isNil,
  pipe,
  reject,
  startsWith,
  toString,
  uniq,
} as const;
