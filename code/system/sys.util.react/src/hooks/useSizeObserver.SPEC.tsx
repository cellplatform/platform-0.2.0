import { Spec } from '../test.ui';

export default Spec.describe('hook.useSizeObserver', (e) => {
  e.it('init', async (e) => {
    const ctx = Spec.ctx(e);

    /**
     * TODO 🐷
     */
    const el = <div>🐷 TODO // useSizeObserver</div>;
    ctx.render(el);
  });
});
