import { DEFAULTS as BASE, Pkg } from '../common';
export * from '../common';

export const DEFAULTS = {
  displayName: `${Pkg.name}:DevSplash`,
  qs: BASE.qs,
  fill: true,
  center: true,
  keyboard: true,
};
