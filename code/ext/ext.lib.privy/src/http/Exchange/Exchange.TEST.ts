import { Test } from '../../test.ui';
import { Exchange } from '.';

export default Test.describe('HTTP: ExchangeRate', (e) => {
  e.it('TODO: rate: ETH → USD', async (e) => {
    /**
     * TODO 🐷
     */
    const res = await Exchange.rate('ETH', 'USD');
  });
});
