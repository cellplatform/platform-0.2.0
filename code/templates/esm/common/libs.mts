/**
 * @external
 */
import { clone, mergeDeepRight } from 'ramda';
export const R = { clone, mergeDeepRight };

/**
 * @system
 */
export { rx, slug, Path, Time, Is } from 'sys.util';

/**
 * @local
 */
export { Pkg } from '../index.pkg.mjs';
