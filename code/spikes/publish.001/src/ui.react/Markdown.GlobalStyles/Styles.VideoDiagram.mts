import { Color, COLORS } from '../common';

export const VideoDiagramStyles = {
  h1: {
    fontSize: 120,
    letterSpacing: -2,
    lineHeight: '0.9em',
  },

  'blockquote p': {
    fontSize: 40,
    lineHeight: '1.4em',
  },
};
